merge: feature/api-stream into main - comprehensive FaaS API and infrastructure overhaul

Major Features:
- New CLI commands: delete, list, logs, schedule, versions
- Complete REST API implementation: schedule, delete, list, logs, stream operations
- SQLite database with persistent storage (functions, invocations, schedules, containers)
- Service layer refactor with dedicated services for each domain
- Container lifecycle management with idle cleanup (ContainerSpleen)
- Structured logging with slog throughout codebase

Infrastructure & DevOps:
- Improved deployment workflow with proper permission handling
- Dedicated faas-engine system user with secure permissions
- Enhanced graceful shutdown with proper cleanup phases
- Increased timeout values for container management
- Fixed tar stream packaging errors during deployment CI/CD

Code Quality:
- Comprehensive test coverage for all new features
- Improved error handling throughout
- Refactored Docker SDK with separate client interfaces
- Proper transaction handling in database operations
- Fixed all linting errors and type safety issues
- Suppressed harmless closed pipe errors in tar packaging

Breaking Changes:
- Database schema now required (SQLite)
- Service user changed from ec2-user to faas-engine
- New required configuration constants
- API responses now from database-backed implementation
